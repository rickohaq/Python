exponential = lambda x: x**2   # defines the function
exponential(2)                 # this computes 2**2 = 4, but the result is not stored or printed

print(exponential)             # this prints info about the function object, not the result
print(exponential(5))  # This will print 4
